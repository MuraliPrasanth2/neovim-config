-- This plugin will help to make the vim.ui.select function to be more user friendly, without this, we would need to specify the option number and click enter,
-- with this we can use CTRL_N and CTRL_P and select using enter
return {
    "stevearc/dressing.nvim",
    event = "VeryLazy",
}
